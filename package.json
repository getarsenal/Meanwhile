{
  "name": "meanwhile",
  "version": "1.0.0",
  "private": true,
  "description": "Meanwhile — interview-process tracker. The web app is the single static index.html (no build). This package.json exists only for the Capacitor iOS wrapper.",
  "scripts": {
    "cap:sync": "cap sync ios",
    "cap:copy": "cap copy ios",
    "cap:open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/local-notifications": "^6.0.0",
    "@capacitor/preferences": "^6.0.0",
    "@capacitor-community/contacts": "^6.0.0",
    "@ebarooni/capacitor-calendar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/assets": "^3.0.0"
  }
}
